// "phone" represents the feature to access phone resource on the
//      server in a RESTful way.
// It's a complete feature, that's why it's in its own module and
//      has its own directory.
// It's a low level module for our application. That's why we name it like this

angular.module("core.phone", ["ngResource"]);